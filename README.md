Jupyter notebook GGNN_training.ipynb contains the workflow for hyperparameter optimization and benchmark
Source code gdnn.py contains the GGNN implementation - to be worked on

Folder papyrus_datasets contains all 10 datasources - named by the target's uniprot ID
Example of a split.json file under random_split.json. The project contains also aggregate_cluster_split.json and cluster_split.json files with the same formatting as random_split.json